import SectionContainer from "@/components/global/SectionContainer";
import SectionTitle from "@/components/global/SectionTitle";

const WhyJoinUs = () => {
  return (
    <SectionContainer verticalPadding>
      <SectionTitle
        title="Build the future of knowledge, together."
        subtitle="AtlasNote is more than software - it's a team of curios builders rethinking how modern teams capture, share, and grow knowledge. Here, you'll find autonomy, impact, an the chance to ship meaningful work every week."
        section="Why Join Us?"
        sectionType="whyJoinUs"
      />
      <div>{}</div>
    </SectionContainer>
  );
};

export default WhyJoinUs;
