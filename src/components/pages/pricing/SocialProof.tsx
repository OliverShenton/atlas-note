import SectionContainer from "@/components/global/SectionContainer";

const socialData = [];

const SocialProof = () => {
  return (
    <SectionContainer id="pricing-social-proof" className="max-w-7xl">
      <div></div>
    </SectionContainer>
  );
};
